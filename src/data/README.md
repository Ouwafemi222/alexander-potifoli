# Content (no backend)

Edit these files to update the site, then redeploy:

- **`portfolio.json`** — add/remove portfolio projects (images go in `public/portfolio/`)
- **`../lib/constants.ts`** — brand info, services, pricing, testimonials

No database or admin panel required.
